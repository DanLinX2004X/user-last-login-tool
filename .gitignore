# .gitignore

# Project Logs and Data Output
# Output reports (as defined in the script)
*.csv
*.log
*.tmp

# Common temporary/log directories used by the script
/opt/sre-reports/
/var/log/sre-tools/
tmp/
logs/
data/

# Build Artifacts (Make, AUR, DEB, RPM)
# General temporary files
*~
*.bak

# Artifacts from 'make'
*.o
*.a

# Temporary directory used for testing package builds
/build_test/

# AUR / Makepkg build artifacts
pkg/
src/
*.pkg.tar.xz
*.tar.gz

# Debian build artifacts
# Final Debian packages
*.deb
# Build directories and temporary files generated by debhelper
debian/debhelper-build-stamp
debian/*.substvars
debian/files
debian/user-last-login-tool
debian/user-last-login-tool.debhelper
debian/user-last-login-tool.postinst.debhelper
debian/user-last-login-tool.postrm.debhelper

# RPM build artifacts
*.rpm
# Standard RPM build directories (if using rpmbuild)
BUILD/
BUILDROOT/
RPMS/
SOURCES/
SPECS/
SRPMS/

# Docker Artifacts
# Docker image contexts
*.tar


# IDE and System Files
# IDE Files (VSCode, IntelliJ, etc.)
.vscode/
.idea/

# System Files
.DS_Store
Thumbs.db

# Python cache (though not a Python project, good practice)
__pycache__/
*.pyc
*.pyo
